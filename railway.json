{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run build && npm run prisma:generate && npm run start",
    "healthcheckPath": "/login",
    "healthcheckTimeout": 120
  }
}
